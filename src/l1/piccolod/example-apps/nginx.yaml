name: nginx-demo
image: nginx:latest
type: user
ports:
  http:
    container: 80
    host: 8080
permissions:
  network:
    internet: allow
    local_network: allow
environment:
  NGINX_PORT: "80"
resources:
  limits:
    memory: 128M
    cpu: 0.5
