name: simple-nginx-integration
image: docker.io/library/nginx:alpine
type: user
subdomain: nginx-test
ports:
  web:
    container: 80
    host: 8090
environment:
  NGINX_HOST: localhost
  NGINX_PORT: "80"